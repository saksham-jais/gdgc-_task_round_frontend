window.onload = function() {

    const fadeInElement = document.querySelector('header');
    fadeInElement.classList.add('visible'); // Trigger the fade-in transition
    const Scle = document.querySelector('.scl');
    Scle.classList.add('visible');
    const Slde = document.querySelector('.slide');
    Slde.classList.add('visible');
  
  };
